# 执行计划：预测消费页面 apply_to 参数逻辑修复

## 任务分解

### Phase 1: 后端修复

#### 1.1 修改 get_forecast_trend 方法

**文件**：`backend/app/services/analytics.py`

**步骤**：
1. 读取当前 `get_forecast_trend` 方法实现
2. 添加月份范围计算逻辑：
   - 根据 `apply_to` 参数确定 start_month
   - 根据 `forecast_months` 参数确定 end_month
   - 处理边界条件（12 月、超过 12 个月等）
3. 修改数据生成循环，只生成指定范围内的月份数据
4. 保持返回数据格式不变

**验证**：
- 运行后端单元测试
- 测试 apply_to='all' 场景
- 测试 apply_to='future_only' 场景
- 测试边界条件

#### 1.2 添加单元测试

**文件**：`backend/tests/test_analytics_service.py`

**步骤**：
1. 添加测试用例：apply_to='all', forecast_months=12
2. 添加测试用例：apply_to='future_only', forecast_months=6
3. 添加测试用例：当前月是 12 月的边界条件
4. 添加测试用例：forecast_months=0
5. 添加测试用例：forecast_months 超过 12

**验证**：
- 所有测试通过
- 覆盖率不降低

### Phase 2: 前端修复

#### 2.1 修改趋势图渲染逻辑

**文件**：`frontend/src/views/analytics/Forecast.vue`

**步骤**：
1. 读取当前趋势图渲染代码
2. 修改 X 轴标签生成逻辑：
   - 从后端返回的数据中提取月份信息
   - 动态生成月份标签（不再固定 1-12 月）
3. 修改图表配置，使用动态标签
4. 确保图表样式适配不同月份数量

**验证**：
- 运行前端类型检查
- 浏览器中验证不同场景
- 测试 apply_to='all' 显示 12 个月
- 测试 apply_to='future_only' 显示指定月份

#### 2.2 更新 API 调用

**文件**：`frontend/src/api/analytics.ts`

**步骤**：
1. 确认 API 接口签名未变
2. 无需修改（保持向后兼容）

**验证**：
- 类型检查通过

### Phase 3: 端到端验证

#### 3.1 浏览器测试

**步骤**：
1. 启动前后端服务
2. 打开预测消费页面
3. 测试场景 1：选择"更新历史及后续月份"
   - 验证图表显示 12 个月
   - 验证数据正确
4. 测试场景 2：选择"仅后续月份"，forecast_months=6
   - 验证图表显示 8-12 月（假设当前是 8 月）
   - 验证数据正确
5. 测试场景 3：修改 forecast_months
   - 验证图表动态更新

**验证**：
- 所有场景通过
- 无控制台错误
- 图表渲染正常

#### 3.2 代码审查

**步骤**：
1. 审查后端代码改动
2. 审查前端代码改动
3. 审查测试用例
4. 确认符合设计文档

**验证**：
- 代码质量符合标准
- 无潜在问题

### Phase 4: 提交与归档

#### 4.1 提交代码

**步骤**：
1. 运行所有测试
2. 运行 lint 检查
3. 提交代码
4. 推送到远程分支

**验证**：
- 所有检查通过
- 代码已推送

#### 4.2 归档任务

**步骤**：
1. 更新任务状态为完成
2. 记录经验教训
3. 归档任务文件

**验证**：
- 任务已归档

## 时间估算

- Phase 1: 后端修复 - 2 小时
- Phase 2: 前端修复 - 1.5 小时
- Phase 3: 端到端验证 - 1 小时
- Phase 4: 提交与归档 - 0.5 小时

**总计**：5 小时

## 风险缓解

1. **充分测试**：每个阶段都有明确的验证步骤
2. **渐进式开发**：先后端后前端，确保接口稳定
3. **代码审查**：提交前进行代码审查
4. **回滚计划**：如果出现问题，可以快速回滚到修复前的版本

## 依赖项

- 无外部依赖
- 使用现有的图表库（ECharts）
- 使用现有的测试框架

## 成功标准

1. 所有测试通过
2. 浏览器验证通过
3. 代码审查通过
4. 用户验收通过
